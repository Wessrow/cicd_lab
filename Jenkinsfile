pipeline {
    
    agent any

    stages {

        stage("build") {
            echo 'test'
        }

        stage("test"){
            echo 'another test'
        }

        stage("deploy"){
            echo 'if success'
        }
    }
}